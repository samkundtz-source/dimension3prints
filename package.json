{
  "name": "map-3d-demo",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "earcut": "^2.2.4",
    "fflate": "^0.8.2",
    "leaflet": "^1.9.4",
    "stripe": "^21.0.1",
    "three": "^0.163.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.31.1",
    "vite": "^6.4.2",
    "wrangler": "^4.81.0"
  }
}
